spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:13306/usercenter-online
    username: root
    password: hitxrx123
  # session 失效时间
  session:
    timeout: 86400
#server:
#  address: 0.0.0.0
